global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: rc-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:3001"]
  - job_name: rc-edge
    metrics_path: /health/live
    static_configs:
      - targets: ["edge:3002"]
