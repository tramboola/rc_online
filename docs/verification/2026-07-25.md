# Verification evidence — 2026-07-25

## Passed locally

- `pnpm typecheck`: all 10 workspace packages passed.
- `pnpm lint`: all TypeScript workspace checks passed.
- `pnpm test`: 15 tasks passed, including 16 failure-acceptance tests and a
  deterministic 100-ride regression.
- `pnpm build`: API, worker, edge, shared packages, and Next.js production build
  passed; web routes `/` and `/[...screen]` were generated.
- Pi protocol pytest: 1/1 passed.
- PlatformIO native Unity: 4/4 passed.
- ESP32-C3 debug and release: passed; release is 4.2% RAM and 18.9% flash.
- FFmpeg NVENC smoke benchmark: 120 frames, 1280×720 at 60 fps, `h264_nvenc`,
  approximately 2.39× realtime on the local RTX 3050.
- `pnpm audit --prod --audit-level high`: no known vulnerabilities after
  upgrading Drizzle and pinning patched transitive versions.
- Official Terraform 1.15.8 archive matched HashiCorp SHA-256; `terraform fmt
  -check -recursive` and `terraform validate` pass with Google 7.41.0 and random
  3.9.0 providers.
- User-authorized Playwright Chromium visual and interaction QA: 8 applicable
  tests passed, 10 project/viewport-inapplicable cases intentionally skipped.
  Seven source-sized desktop/wide captures and four Pixel 7 captures have no
  unexpected console errors; mobile captures have no horizontal overflow.
- Four reference-comparison iterations closed all actionable P0/P1/P2 visual
  findings. Evidence and the final disposition are in `design-qa.md`.
- The owner dispositioned hardware gates as `WAIVED_FOR_SIMULATION` and legal
  gates as `OWNER_ACCEPTED_DEFERRED`, strictly for internal simulation. This is
  not physical/legal evidence and does not permit `LAUNCH READY`.
- WSL was upgraded from the legacy inbox implementation to `2.7.11.0` with
  kernel `6.18.33.2-2`.
- Docker Desktop `4.83.0.234302` was installed in per-user WSL2 mode. Engine
  `29.6.2`, Compose `v5.3.1`, client/server negotiation, and the official
  `hello-world` container passed. The RC Racing Compose file passes validation
  for all three profiles. Full evidence is in
  `docs/verification/docker-desktop-2026-07-25.md`.
- On 2026-07-27 the complete `core/sim/obs` stack was built and started.
  PostgreSQL and Redis are healthy, the migration job exited 0 after applying
  `0001_simulation_core.sql` and `0002_ledger_immutability.sql`, and API,
  worker, and web health checks pass. Edge, Coturn, MediaMTX, Mailpit,
  Prometheus, Grafana, Loki, and Tempo are running with zero restart counts.
- Local HTTP verification returned 200 for web, API readiness, OpenAPI JSON,
  edge readiness, Mailpit, Prometheus readiness, and Grafana health. Detailed
  evidence is in `docs/verification/compose-2026-07-27.md`.

## Not yet run on this host

- Redis/BullMQ webhook processing, edge reconnect/outbox replay, forced TURN
  relay, and full media-path acceptance have not yet been exercised as one
  integrated scenario.
- k6, gitleaks, Trivy, and Semgrep CLIs are absent; their CI definitions exist
  but have not run in this working tree.
- No GCP staging project, Sentry project, Stripe sandbox, Google OAuth client,
  physical car, Pi, ESP32 board, camera, or timing system was available.
- No external legal approval was obtained. The owner-only simulation waiver is
  recorded separately and cannot be reused as launch approval.
